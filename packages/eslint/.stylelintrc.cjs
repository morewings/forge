// In this package context this config is just dummy to silence Webstorm false error reports
module.exports = {
  ignoreFiles: ['**/*.snap', '**/*.json', '**/*.png', '**/*.md', '**/*.woff', '**/*.ttf', '**/*.eot']
}
